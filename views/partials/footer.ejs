  </main>

  <!-- ===== FOOTER ===== -->
  <footer class="footer">
    <div class="footer__container container">
      <div class="footer__grid">

        <!-- Column 1: About -->
        <div class="footer__col">
          <h3 class="footer__heading">Талия</h3>
          <p class="footer__tagline"><%= content.site.tagline %></p>
          <p class="footer__company"><%= content.site.company %></p>
          <% if (content.site.inn) { %>
            <p class="footer__requisites">ИНН <%= content.site.inn %><% if (content.site.ogrn) { %> · ОГРН <%= content.site.ogrn %><% } %><% if (content.site.kpp) { %> · КПП <%= content.site.kpp %><% } %></p>
          <% } %>
          <% if (content.site.legalAddress) { %>
            <p class="footer__requisites">Юр. адрес: <%= content.site.legalAddress %></p>
          <% } %>
          <p class="footer__license">
            Лицензия: <%= content.site.license %><% if (content.site.licenseIssuer) { %>,
            выдана <%= content.site.licenseIssuer %><% } %><% if (content.site.licenseDate) { %> <%= content.site.licenseDate %><% } %>
          </p>
        </div>

        <!-- Column 2: Quick Navigation -->
        <div class="footer__col">
          <h3 class="footer__heading">Навигация</h3>
          <ul class="footer__links">
            <li>
              <a href="/about" class="footer__link">О нас</a>
            </li>
            <li>
              <a href="/team" class="footer__link">Специалисты</a>
            </li>
            <li>
              <a href="/gallery" class="footer__link">Галерея</a>
            </li>
            <li>
              <a href="/promotions" class="footer__link">Акции</a>
            </li>
          </ul>
        </div>

        <!-- Column 3: Contacts -->
        <div class="footer__col">
          <h3 class="footer__heading">Контакты</h3>
          <ul class="footer__contacts">
            <li class="footer__contact-item">
              <i data-lucide="map-pin" class="footer__contact-icon"></i>
              <span><%= content.site.address %></span>
            </li>
            <li class="footer__contact-item">
              <i data-lucide="phone" class="footer__contact-icon"></i>
              <span>
                <a href="tel:<%= content.site.phoneRaw %>" class="footer__link"><%= content.site.phone %></a>
                <% if (content.site.phone2) { %>
                  <br><a href="tel:<%= content.site.phoneRaw2 %>" class="footer__link"><%= content.site.phone2 %></a>
                <% } %>
              </span>
            </li>
            <li class="footer__contact-item">
              <i data-lucide="clock" class="footer__contact-icon"></i>
              <span><%= content.site.hours %></span>
            </li>
          </ul>
        </div>

        <!-- Column 4: Social -->
        <div class="footer__col">
          <h3 class="footer__heading">Мы в соцсетях</h3>
          <div class="footer__socials">
            <% if (content.site.vk) { %>
              <a href="<%= content.site.vk %>" target="_blank" rel="noopener" class="footer__social" aria-label="ВКонтакте">
                <i data-lucide="users"></i>
                <span>ВКонтакте</span>
              </a>
            <% } %>
            <% if (content.site.max) { %>
              <a href="<%= content.site.max %>" target="_blank" rel="noopener" class="footer__social" aria-label="МАКС">
                <i data-lucide="message-circle"></i>
                <span>МАКС</span>
              </a>
            <% } %>
          </div>
        </div>

      </div>

      <!-- Medical disclaimer (ФЗ-38 «О рекламе», ст. 24) -->
      <div class="footer__disclaimer">
        <p class="med-disclaimer med-disclaimer--footer">Имеются противопоказания. Необходима консультация специалиста.</p>
        <p class="footer__authorities">
          Контролирующие органы:
          <a href="https://roszdravnadzor.gov.ru" target="_blank" rel="noopener">Росздравнадзор</a> 8 (800) 550-99-03 ·
          <a href="https://rospotrebnadzor.ru" target="_blank" rel="noopener">Роспотребнадзор</a> 8 (800) 555-49-43 ·
          <a href="https://mz.mosreg.ru" target="_blank" rel="noopener">Минздрав МО</a> 8 (495) 777-73-72
        </p>
      </div>

      <!-- Bottom bar -->
      <div class="footer__bottom">
        <p class="footer__copyright">
          &copy; <%= new Date().getFullYear() %> <%= content.site.company %>. Все права защищены.
          <span class="footer__age-mark" title="Информация не предназначена для лиц младше 18 лет">18+</span>
        </p>
        <div class="footer__legal-links">
          <a href="/privacy" class="footer__legal-link">Политика конфиденциальности</a>
          <a href="/terms" class="footer__legal-link">Пользовательское соглашение</a>
          <a href="/consent" class="footer__legal-link">Согласие на обработку данных</a>
          <a href="/offer" class="footer__legal-link">Договор оферты</a>
        </div>
      </div>
    </div>
  </footer>

  <!-- ===== FLOATING BUTTONS ===== -->

  <!-- MAX messenger floating button (bottom-left) -->
  <% if (content.site.max) { %>
    <a href="<%= content.site.max %>" target="_blank" rel="noopener" class="whatsapp-float" aria-label="Написать в МАКС">
      <i data-lucide="message-circle"></i>
    </a>
  <% } %>

  <!-- ProfSalon booking button (bottom-right) -->
  <% if (content.site.profsalon) { %>
    <a href="<%= content.site.profsalon %>" target="_blank" rel="noopener" class="booking-float">
      <i data-lucide="calendar-check"></i>
      <span>Записаться онлайн</span>
    </a>
  <% } %>

  <!-- ProfSalon Widget -->
  <% if (content.site.profsalonWidget) { %>
    <%- content.site.profsalonWidget %>
  <% } %>

  <!-- Cookie Consent Banner -->
  <div class="cookie-banner" id="cookieBanner" style="display:none;">
    <div class="cookie-banner__content">
      <p>Наш сайт использует файлы куки (cookies) для улучшения работы сайта и анализа трафика. Подробнее — в <a href="/privacy" class="link-inline">Политике конфиденциальности</a>.</p>
      <div class="cookie-banner__actions">
        <button class="cookie-banner__btn" id="cookieAccept">Принять</button>
        <button class="cookie-banner__btn cookie-banner__btn--ghost" id="cookieDecline">Отклонить</button>
      </div>
    </div>
  </div>

  <!-- Scripts -->
  <script src="/js/main.js?v=20260714"></script>
  <script>
    // Initialize Lucide icons (script is deferred — wait for it)
    document.addEventListener('DOMContentLoaded', function() {
      if (window.lucide) { try { lucide.createIcons(); } catch (e) {} }
    });

    // Cookie consent (accept / decline)
    (function() {
      var banner = document.getElementById('cookieBanner');
      var btnAccept = document.getElementById('cookieAccept');
      var btnDecline = document.getElementById('cookieDecline');
      if (!localStorage.getItem('cookieConsent')) {
        banner.style.display = 'flex';
      }
      function close(value) {
        localStorage.setItem('cookieConsent', value);
        banner.style.display = 'none';
      }
      btnAccept.addEventListener('click', function() { close('accepted'); });
      btnDecline.addEventListener('click', function() { close('declined'); });
    })();
  </script>
</body>
</html>
